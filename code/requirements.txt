# The versions the recorded runs were produced under.
#
# The manuscript's tables quote wall-clock times, so the library that does the
# dense eigensolve is part of the measurement: a different LAPACK will move the
# seconds, and a different truncation driver can move the retained rank by a few
# directions at the 1e-12 threshold.  Pinning is therefore not pedantry here.
#
# Run from the code directory:
#     python -m pip install -r requirements.txt
#     python -m pip install -e . --no-deps
#
# python 3.12.13

numpy==2.5.1
scipy==1.18.0
matplotlib==3.10.8
pandas==3.0.1
threadpoolctl==3.5.0

# the isoparametric P2 finite element baseline of Experiment 2
scikit-fem==12.0.1

# the neural baselines of Experiments 3 and 4, and the Sobol generator whose
# scrambles Experiment 3's integration rule is validated on
torch==2.11.0

# development only
pytest==9.1.1
ruff==0.15.21
